{
  "name": "drishti-ai",
  "version": "2.0.0",
  "private": true,
  "scripts": {
    "build": "npm run build --prefix frontend",
    "dev": "npm run dev --prefix frontend"
  },
  "devDependencies": {
    "@playwright/test": "^1.63.0"
  },
  "allowScripts": {
    "core-js": true,
    "esbuild": true
  },
  "lavamoat": {
    "allowScripts": {
      "core-js": true,
      "esbuild": true
    }
  },
  "overrides": {
    "uuid": "^11.1.0"
  }
}
